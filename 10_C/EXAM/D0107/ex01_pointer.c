/* *******************************************************************
* File name    : ex01_pointer.c
* Description  : 
* Author       : HyeonWoo
* History      : 2026.01.07 HyeonWoo
* ********************************************************************/
// -------------------------------------------------------------------
// 1. 전처리기 - 헤더파일 포함 
// -------------------------------------------------------------------
#include <stdio.h>

// -------------------------------------------------------------------
// 2. 매크로 상수 정의 
// -------------------------------------------------------------------

// -------------------------------------------------------------------
// 3. 구조체 정의 (필요 시) 
// -------------------------------------------------------------------

// -------------------------------------------------------------------
// 4. 전역 변수/상수 선언 (Global Variable / constant)
// -------------------------------------------------------------------

// -------------------------------------------------------------------
// 5. 함수 선언 
// -------------------------------------------------------------------

// -------------------------------------------------------------------
// 6. 메인 함수 
// -------------------------------------------------------------------
int main(void)
{

	// 변수 선언
	int value = 127;	// 데이터 저장 변수
	char   ch = 'A';

	int* p;				// 데이터 주소 저장 포인터 변수
	p = &value;			// 포인터 변수에 value 변수의 주소 저장

	int*  p2 = &value;

	char* p3 = &ch;

	
	// 입력

	// 처리

	// 출력
	printf("value                               읽기 : %d\n", value);
	printf("value 주소                          읽기 : %p\n\n", &value);

	printf("value 주소를 저장한 포인터 변수 읽기  p  : %p\n", p);
	printf("포인터 변수를 역참조해 value 값 읽기 *p  : %d\n", *p);`

	printf("value 주소를 저장한 포인터 변수 읽기  p2 : %p\n", p2);
	printf("포인터 변수를 역참조해 value 값 읽기 *p2 : %d\n\n", *p2);

	// 변수들의 데이터 크기 확인 : sizeof() 연산자
	printf("변수 value의 크기 : %zu bytes\n", sizeof(value));
	printf("변수    ch의 크기 : %zu bytes\n", sizeof(ch));
	printf("변수    p1의 크기 : %zu bytes\n", sizeof(p));
	printf("변수    p2의 크기 : %zu bytes\n", sizeof(p2));
	printf("변수    p3의 크기 : %zu bytes\n", sizeof(p3));

	return 0;
}
// -------------------------------------------------------------------
// 7. 함수 구현 
// -------------------------------------------------------------------
